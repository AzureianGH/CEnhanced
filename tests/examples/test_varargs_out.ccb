ccbytecode 2

.extern printf params=(ptr) returns=i32 varargs

.func main ret=i32 params=0 locals=0
  const_str "Two args work fine, %d and %d.\n"
  const i32 42
  const i32 7
  call printf i32 (ptr,i32,i32)
  drop i32
  const_str "Hello %s, your score is %d out of %d.\n"
  const_str "Alice"
  const i32 95
  const i32 100
  call printf i32 (ptr,ptr,i32,i32)
  drop i32
  const i32 0
  ret
.endfunc
