extend from "C" i32 printf(char *, _vaargs_);
extend from "C" i32 strcmp(char *, char *);

enum Property
{
    PropertyA,
    PropertyB,
    PropertyC
};

fun main(i32 argc, char** argv) -> i32
{
    if (argc < 2)
    {
        printf("Usage: %s <property>\n", argv[0]);
        ret 1;
    }
    
    i32[] prop = [3, 4, 5];

    i32[] newprop = new i32[3];

    char[][] test = new char[3][20];
    ret 0;
}