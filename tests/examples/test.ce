extend from "C" i32 printf(char *, _vaargs_);
extend from "C" i32 strcmp(char *, char *);
extend from "C" void *malloc(int);

enum Property
{
    PropertyA,
    PropertyB,
    PropertyC
};

fun main(i32 argc, char** argv) -> i32
{
    i32[][] prop = i32[3][3]; // 2D array
    prop[0][0] = 1;
    prop[0][1] = 2;
    prop[0][2] = 3;
    prop[1][0] = 4;
    prop[1][1] = 5;
    prop[1][2] = 6;
    prop[2][0] = 7;
    prop[2][1] = 8;
    prop[2][2] = 9;
    
    for (i32 i = 0; i < 3; i++)
    {
        for (i32 j = 0; j < 3; j++)
        {
            printf("%d ", prop[i][j]);
        }
        printf("\n");
    }

    delete prop; // Free the 2D array

    ret 0;
}