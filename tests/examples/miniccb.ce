extend from "C" i32 printf(char *, _vaargs_);
extend from "C" void *malloc(int);

struct TestThing
{
    i32 a;
    u8 b;
};

[EntryPoint]
fun main() -> i32
{
    f32 val = (4 as f32);
    printf("Size of TestThing: %d\n", sizeof(TestThing));
    ret 0;
}