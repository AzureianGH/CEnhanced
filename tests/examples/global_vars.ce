constant i32 base = 5;
i32 counter;
bool flag;
constant i32* globalPtr = null;

fun main() -> i32 {
    counter = base;
    flag = counter == 5;
    if (flag) {
        counter = counter + 7;
    }
    ++counter;
    counter--;
    return counter;
}
