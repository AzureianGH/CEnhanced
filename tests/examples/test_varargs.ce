module Program;

extend from "C" i32 printf(char *, _vaargs_);

[EntryPoint]
fun main() -> i32
{
    printf("Two args work fine, %d and %d.\n", 42, 7);
    printf("Hello %s, your score is %d out of %d.\n", "Alice", 95, 100);
    ret 0;
}