managed module Tmp.ManagedSplitLen;

bring Std.IO as IO;
bring Std.String.Extensions;

entrypoint expose fun main() -> i32
{
    string input = IO.ReadLine();
    string[] parts = input.Split();
    for (int i = 0; i < parts.length; i++) {
        parts[i] = parts[i].Trim();
    }
    ret 0;
}
